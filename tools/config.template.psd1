@{
    # Name of app as it will be displayed in AzureAD and on user certs
    ModuleName = "PSTask"

    # PowerShell Repository name
    RepoName = "POWERSHELL_REPO_NAME"
    RepoPath = "POWERSHELL_REPO_PATH"
}